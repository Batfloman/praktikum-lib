[project]
name = "batfloman-praktikum-lib"
version = "0.1.0"
description = "My test package"
authors = [{ name = "Florian Hirche" }]
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
